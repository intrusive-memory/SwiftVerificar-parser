# Gemini Instructions

See [AGENTS.md](AGENTS.md) for library usage guidelines, public API reference, and development instructions.

## Build Commands
- Build: `xcodebuild build -scheme SwiftVerificarParser -destination 'platform=macOS'`
- Test: `xcodebuild test -scheme SwiftVerificarParser -destination 'platform=macOS'`
- NEVER use `swift build` or `swift test`
